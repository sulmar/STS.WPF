<UserControl x:Class="STS.Ikar.WPFClient.UserControls.AddressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:STS.Ikar.WPFClient.UserControls"
             xmlns:uc="clr-namespace:STS.Ikar.WPFClient.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.DataContext>
        <uc:AddressViewModel />
    </UserControl.DataContext>
    
    <Grid>

        <StackPanel>
            <TextBox Text="{Binding City}" />
            <TextBox Text="{Binding Street}" />
            <TextBox Text="{Binding Postcode}" />
        </StackPanel>


    </Grid>
</UserControl>
